[Definition]
# Matches structured pyBulletin authentication failures written to
# /var/log/pybulletin/authfail.log for web paths (public web, sysop console).
failregex = ^.*pybulletin AUTHFAIL channel=(?:web|sysop-web) ip=<HOST> call=[^ ]+ reason=[a-z_]+$

ignoreregex =
