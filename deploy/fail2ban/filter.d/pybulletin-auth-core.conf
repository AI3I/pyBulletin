[Definition]
# Matches structured pyBulletin authentication failures written to
# /var/log/pybulletin/authfail.log for core-service paths (telnet, AX.25).
failregex = ^.*pybulletin AUTHFAIL channel=(?:telnet|ax25) ip=<HOST> call=[^ ]+ reason=[a-z_]+$

ignoreregex =
